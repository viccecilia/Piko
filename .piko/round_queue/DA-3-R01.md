# Round ID: DA-3-R01

Round Name: Candidate Pipeline Request

本轮目标:
Convert an article candidate into an article workflow request without publishing.

本轮任务:
- 执行任务:
  - Add helper that maps article candidate to `ArticleWorkflowRequest` or equivalent.
  - Include article intent and safety metadata where supported.
- 测试任务:
  - Test candidate request contains game name and player question.
- 协作验收任务:
  - Worker summary must include request JSON.

允许修改:
- `packages/discovery/*`
- `packages/workflows/*`
- `packages/shared/schemas.py`
- `tests/test_discovery_search.py`
- `.piko/summaries/worker_DA-3-R01.md`
- `.piko/round_status.json`

禁止修改:
- Do not auto-run real source collectors.
- Do not publish.

必须运行的验证:
- `python -m pytest tests\test_discovery_search.py -q`
- `python -m packages.workflows.article_pipeline`

完成定义:
- Candidate can be converted into a workflow request safely.

输出格式:
- 修改了什么
- 每个任务状态
- 验证结果
- 协作验收结果
- 未完成/风险
- 下一轮建议
