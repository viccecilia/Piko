# Round ID: DA-3-R02

Round Name: Candidate Workflow Runner

本轮目标:
Run the article workflow for a candidate in fixture-safe mode.

本轮任务:
- 执行任务:
  - Add a function that accepts an article candidate and returns workflow result.
  - Default must use fixture-safe behavior and no real collection.
- 测试任务:
  - Test workflow completes for one publish candidate.
  - Test `publishing_performed=false`.
- 协作验收任务:
  - Worker summary must include workflow status and verification status.

允许修改:
- `packages/workflows/*`
- `packages/discovery/*`
- `tests/test_discovery_search.py`
- `tests/test_content_benchmark.py`
- `.piko/summaries/worker_DA-3-R02.md`
- `.piko/round_status.json`

禁止修改:
- Do not default-enable real connectors.
- Do not call LLM by default.
- Do not publish.

必须运行的验证:
- `python -m pytest tests\test_discovery_search.py tests\test_content_benchmark.py -q`
- `python -m packages.workflows.article_pipeline`

完成定义:
- Candidate can run through article workflow safely in tests.

输出格式:
- 修改了什么
- 每个任务状态
- 验证结果
- 协作验收结果
- 未完成/风险
- 下一轮建议
