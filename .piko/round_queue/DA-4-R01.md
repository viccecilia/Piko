# Round ID: DA-4-R01

Round Name: Candidate Draft Artifact

本轮目标:
Save candidate workflow output as a draft artifact for operator review.

本轮任务:
- 执行任务:
  - Add artifact writer for discovery-to-article candidate runs.
  - Artifact must include candidate, workflow result, draft, sources, evidence, agent trace, verification report, and safety fields.
- 测试任务:
  - Test artifact JSON is written to a safe output dir.
  - Test top-level `publish_ready=false` and `publishing_performed=false`.
- 协作验收任务:
  - Worker summary must include artifact path.

允许修改:
- `packages/workflows/*`
- `artifacts/*`
- `tests/test_discovery_search.py`
- `.piko/summaries/worker_DA-4-R01.md`
- `.piko/round_status.json`

禁止修改:
- Do not publish artifact.
- Do not store long raw sources.

必须运行的验证:
- `python -m pytest tests\test_discovery_search.py -q`

完成定义:
- Candidate run can save a safe artifact.

输出格式:
- 修改了什么
- 每个任务状态
- 验证结果
- 协作验收结果
- 未完成/风险
- 下一轮建议
