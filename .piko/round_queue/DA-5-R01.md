# Round ID: DA-5-R01

Round Name: Candidate Pipeline CLI

本轮目标:
Add a CLI for running discovery-to-article candidate flow in fixture-safe mode.

本轮任务:
- 执行任务:
  - Add or extend a CLI command to select a discovery candidate and run article draft generation.
  - Defaults must be fixture/offline and non-LLM.
- 测试任务:
  - Smoke test CLI with Stardew publish candidate.
- 协作验收任务:
  - Worker summary must include CLI command and output paths.

允许修改:
- `packages/discovery/*`
- `packages/workflows/*`
- `tests/test_discovery_search.py`
- `docs/player_pain_discovery.md`
- `.piko/summaries/worker_DA-5-R01.md`
- `.piko/round_status.json`

禁止修改:
- Do not default-enable real collection or LLM.
- Do not publish.

必须运行的验证:
- `python -m pytest tests\test_discovery_search.py -q`
- CLI command added by this round

完成定义:
- Operator can run candidate draft generation from CLI safely.

输出格式:
- 修改了什么
- 每个任务状态
- 验证结果
- 协作验收结果
- 未完成/风险
- 下一轮建议
