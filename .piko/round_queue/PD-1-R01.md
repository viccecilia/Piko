# Round ID: PD-1-R01

Round Name: Discovery Scoring Contract

本轮目标:
Turn the discovery funnel scoring rules into a stable, tested contract.

本轮任务:
- 执行任务:
  - Review `GameHeatSignal`, `PlayerQuestionSignal`, `PlayerNeedCluster`, and `DiscoveryDecision`.
  - Document score inputs: game heat, question heat, answer status, evidence quality, conflict, risk, freshness, evergreen value, competition gap, and Piko value add.
  - Add missing schema fields only if required by the contract.
- 测试任务:
  - Add tests for score boundaries and decision outputs.
- 协作验收任务:
  - Worker summary must include the scoring matrix.

允许修改:
- `packages/shared/schemas.py`
- `packages/discovery/scoring.py`
- `tests/test_discovery_search.py`
- `docs/player_pain_discovery.md`
- `.piko/summaries/worker_PD-1-R01.md`
- `.piko/round_status.json`

禁止修改:
- Do not add real collectors.
- Do not publish or deploy.
- Do not make tests depend on network.

必须运行的验证:
- `python -m pytest tests\test_discovery_search.py -q`
- `python -m packages.discovery.search_cli --min-game-heat 50 --limit 5`

完成定义:
- Scoring contract is documented and covered by tests.

输出格式:
- 修改了什么
- 每个任务状态
- 验证结果
- 协作验收结果
- 未完成/风险
- 下一轮建议
