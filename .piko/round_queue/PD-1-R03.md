# Round ID: PD-1-R03

Round Name: Discovery Result Contract

本轮目标:
Make discovery output suitable for downstream article pipeline candidates.

本轮任务:
- 执行任务:
  - Confirm cluster output includes article intent, representative questions, source diversity, reasons, next action, and safety decision.
  - Add or refine `DiscoverySearchResponse` fields if needed.
- 测试任务:
  - Test API and CLI return stable JSON fields.
- 协作验收任务:
  - Worker summary must include a sample JSON cluster.

允许修改:
- `packages/shared/schemas.py`
- `packages/discovery/search_engine.py`
- `apps/api/routes/discovery.py`
- `tests/test_discovery_search.py`
- `.piko/summaries/worker_PD-1-R03.md`
- `.piko/round_status.json`

禁止修改:
- Do not trigger article generation from discovery yet.
- Do not publish.

必须运行的验证:
- `python -m pytest tests\test_discovery_search.py -q`
- `python -m packages.discovery.search_cli --query stardew --limit 3`

完成定义:
- Discovery output can be consumed by future article candidate pipeline.

输出格式:
- 修改了什么
- 每个任务状态
- 验证结果
- 协作验收结果
- 未完成/风险
- 下一轮建议
