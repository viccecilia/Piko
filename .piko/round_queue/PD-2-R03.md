# Round ID: PD-2-R03

Round Name: Hot Game API Filters

本轮目标:
Expose useful hot-game filters through discovery search.

本轮任务:
- 执行任务:
  - Ensure `/discovery/search` supports min game heat and region/source filtering.
  - Document example requests.
- 测试任务:
  - Test `min_game_heat` and region filters.
- 协作验收任务:
  - Worker summary must include API examples.

允许修改:
- `apps/api/routes/discovery.py`
- `packages/discovery/search_engine.py`
- `tests/test_discovery_search.py`
- `docs/player_pain_discovery.md`
- `.piko/summaries/worker_PD-2-R03.md`
- `.piko/round_status.json`

禁止修改:
- Do not add UI yet.
- Do not trigger network.

必须运行的验证:
- `python -m pytest tests\test_discovery_search.py -q`

完成定义:
- Hot-game filtering works through API and tests.

输出格式:
- 修改了什么
- 每个任务状态
- 验证结果
- 协作验收结果
- 未完成/风险
- 下一轮建议
