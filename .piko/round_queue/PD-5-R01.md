# Round ID: PD-5-R01

Round Name: Answer State Classifier

本轮目标:
Make answered, unanswered, conflicting, partial, and unknown states explicit and tested.

本轮任务:
- 执行任务:
  - Review `answer_status_for`.
  - Add tests for every answer state.
- 测试任务:
  - Confirm accepted/official answers, conflicts, weak evidence, and empty inputs classify correctly.
- 协作验收任务:
  - Worker summary must map answer states to downstream decisions.

允许修改:
- `packages/discovery/scoring.py`
- `tests/test_discovery_search.py`
- `.piko/summaries/worker_PD-5-R01.md`
- `.piko/round_status.json`

禁止修改:
- Do not treat all community replies as answers.
- Do not promote weak evidence to answered.

必须运行的验证:
- `python -m pytest tests\test_discovery_search.py -q`

完成定义:
- Answer-state classifier is deterministic and tested.

输出格式:
- 修改了什么
- 每个任务状态
- 验证结果
- 协作验收结果
- 未完成/风险
- 下一轮建议
