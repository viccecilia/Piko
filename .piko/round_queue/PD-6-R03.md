# Round ID: PD-6-R03

Round Name: Watchlist Output API

本轮目标:
Expose watchlist-ready clusters through discovery filters.

本轮任务:
- 执行任务:
  - Ensure `/discovery/search` can filter `watchlist_waiting_for_answer`.
  - Add docs and tests for watchlist queries.
- 测试任务:
  - Test API returns only watchlist decisions when requested.
- 协作验收任务:
  - Worker summary must include API payload and response summary.

允许修改:
- `apps/api/routes/discovery.py`
- `packages/discovery/search_engine.py`
- `tests/test_discovery_search.py`
- `docs/player_pain_discovery.md`
- `.piko/summaries/worker_PD-6-R03.md`
- `.piko/round_status.json`

禁止修改:
- Do not create scheduler.
- Do not create notification automation.

必须运行的验证:
- `python -m pytest tests\test_discovery_search.py -q`

完成定义:
- Operators can query watchlist candidates safely.

输出格式:
- 修改了什么
- 每个任务状态
- 验证结果
- 协作验收结果
- 未完成/风险
- 下一轮建议
