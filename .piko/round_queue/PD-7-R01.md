# Round ID: PD-7-R01

Round Name: Cluster To Article Intent

本轮目标:
Convert discovery clusters into article pipeline candidates.

本轮任务:
- 执行任务:
  - Add helper to map `PlayerNeedCluster` to game name, player question, article intent, and source search hints.
  - Do not run article generation automatically.
- 测试任务:
  - Test publish_candidate and conflict_explainer mappings.
- 协作验收任务:
  - Worker summary must include a cluster-to-article candidate example.

允许修改:
- `packages/discovery/*`
- `packages/shared/schemas.py`
- `tests/test_discovery_search.py`
- `docs/player_pain_discovery.md`
- `.piko/summaries/worker_PD-7-R01.md`
- `.piko/round_status.json`

禁止修改:
- Do not auto-run writer pipeline.
- Do not publish.

必须运行的验证:
- `python -m pytest tests\test_discovery_search.py -q`
- `python -m packages.workflows.article_pipeline`

完成定义:
- Discovery can produce article candidates without executing publishing.

输出格式:
- 修改了什么
- 每个任务状态
- 验证结果
- 协作验收结果
- 未完成/风险
- 下一轮建议
