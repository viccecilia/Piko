# Round ID: PD-8-R02

Round Name: Discovery CLI View

本轮目标:
Improve CLI output for quick operator inspection.

本轮任务:
- 执行任务:
  - Add optional summary/table mode if useful.
  - Preserve JSON output as default or provide explicit flag.
- 测试任务:
  - Smoke test CLI command.
- 协作验收任务:
  - Worker summary must include CLI examples.

允许修改:
- `packages/discovery/search_cli.py`
- `tests/test_discovery_search.py`
- `docs/player_pain_discovery.md`
- `.piko/summaries/worker_PD-8-R02.md`
- `.piko/round_status.json`

禁止修改:
- Do not require frontend dependencies.
- Do not touch network.

必须运行的验证:
- `python -m packages.discovery.search_cli --min-game-heat 50 --limit 5`
- `python -m pytest tests\test_discovery_search.py -q`

完成定义:
- CLI is useful and keeps machine-readable output available.

输出格式:
- 修改了什么
- 每个任务状态
- 验证结果
- 协作验收结果
- 未完成/风险
- 下一轮建议
