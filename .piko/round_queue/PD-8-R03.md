# Round ID: PD-8-R03

Round Name: Discovery UI Placeholder

本轮目标:
Optionally expose discovery results in an internal lightweight view.

本轮任务:
- 执行任务:
  - If consistent with current app, add a minimal `/discovery/window` or document why API/CLI is enough for now.
  - Keep UI internal and non-publishing.
- 测试任务:
  - Test route loads if added.
- 协作验收任务:
  - Worker summary must explain UI choice.

允许修改:
- `apps/api/routes/discovery.py`
- `apps/web/*`
- `tests/test_discovery_search.py`
- `docs/player_pain_discovery.md`
- `.piko/summaries/worker_PD-8-R03.md`
- `.piko/round_status.json`

禁止修改:
- Do not create public publishing UI.
- Do not make a marketing page.
- Do not add heavy frontend framework unless explicitly justified.

必须运行的验证:
- `python -m pytest tests\test_discovery_search.py -q`

完成定义:
- Operator can inspect discovery output through API/CLI and optional internal view.

输出格式:
- 修改了什么
- 每个任务状态
- 验证结果
- 协作验收结果
- 未完成/风险
- 下一轮建议
