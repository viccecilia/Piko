# Round ID: PD-9-R02

Round Name: Real Source Opt-In Interface

本轮目标:
Prepare opt-in flags and collector interface for one discovery source.

本轮任务:
- 执行任务:
  - Add disabled-by-default interface for selected source if needed.
  - Require explicit live-test flag for network.
  - Return normalized `PlayerQuestionSignal` records.
- 测试任务:
  - Mock HTTP or fixture tests only.
- 协作验收任务:
  - Worker summary must prove normal pytest is offline.

允许修改:
- `packages/collectors/*`
- `packages/discovery/*`
- `packages/shared/config.py`
- `tests/test_discovery_search.py`
- `.piko/summaries/worker_PD-9-R02.md`
- `.piko/round_status.json`

禁止修改:
- Do not default-enable real collection.
- Do not write crawler behavior.
- Do not store full posts.

必须运行的验证:
- `python -m pytest tests\test_discovery_search.py -q`
- `python -m pytest`

完成定义:
- One real-source interface exists behind opt-in and mock tests.

输出格式:
- 修改了什么
- 每个任务状态
- 验证结果
- 协作验收结果
- 未完成/风险
- 下一轮建议
