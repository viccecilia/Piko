# Round ID: PD-9-R03

Round Name: Real Source Live Smoke

本轮目标:
Run a minimal live discovery smoke only when explicitly opted in.

本轮任务:
- 执行任务:
  - Add or run a live smoke that collects only metadata/short snippets.
  - Skip cleanly if opt-in env vars are missing.
- 测试任务:
  - Normal pytest must skip live smoke.
  - Optional live command must be recorded in summary.
- 协作验收任务:
  - Worker summary must include whether live request happened and what was collected.

允许修改:
- `tests/*`
- `.piko/summaries/worker_PD-9-R03.md`
- `.piko/round_status.json`

禁止修改:
- Do not save long raw text.
- Do not crawl multiple pages.
- Do not publish.

必须运行的验证:
- `python -m pytest tests\test_discovery_search.py -q`
- `python -m pytest`

完成定义:
- Live smoke is opt-in, bounded, and skipped by default.

输出格式:
- 修改了什么
- 每个任务状态
- 验证结果
- 协作验收结果
- 未完成/风险
- 下一轮建议
