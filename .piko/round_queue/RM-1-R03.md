# Round ID: RM-1-R03

Round Name: Real Source Normalization Schema

本轮目标:

Create a stable normalization layer that converts source-specific records into Piko hot-game and player-question signals.

本轮任务:
- 执行任务:
  - Normalize hot game signals into `GameHeatSignal`.
  - Normalize player question records into `PlayerQuestionSignal`.
  - Add source summary metadata showing which source returned how many games/questions.
  - Ensure snippets are truncated and metadata excludes raw content fields.
- 测试任务:
  - Test Steam-like, Reddit-like, JP-like, KR-like, and SERP-like mock payloads normalize successfully.
  - Test snippets stay bounded.
  - Test `raw_text`, `body`, `selftext`, `content`, credentials, and authorization fields are not retained.
- 协作验收任务:
  - Worker summary must include one normalized example for a hot game and one for a player question.

允许修改:

- `packages/discovery/*`
- `packages/shared/schemas.py`
- `tests/test_discovery_search.py`
- `.piko/summaries/worker_RM-1-R03.md`
- `.piko/summaries/worker_RM-1.md`
- `.piko/round_status.json`

禁止修改:

- Do not run live collection.
- Do not save real source artifacts.
- Do not publish or deploy.

必须运行的验证:

- `python -m pytest tests\test_discovery_search.py -q`
- `python -m pytest`

完成定义:

- RM-1 source contract, opt-in policy, and normalization schema are implemented and tested.
- Stage summary `.piko/summaries/worker_RM-1.md` exists.
- Status is ready for Piko-verify with `next_round=RM-2-R01`.

输出格式:
- 修改了什么
- 每个任务状态
- 验证结果
- 协作验收结果
- 未完成/风险
- 下一轮建议
