# Round ID: RM-2-R03

Round Name: JP And KR Community Connectors

本轮目标:

Add controlled JP/KR community connector contracts and Unicode-safe normalization.

本轮任务:
- 执行任务:
  - Add JP community adapter behind explicit opt-in and configured endpoint.
  - Add KR community adapter behind explicit opt-in and configured endpoint.
  - Preserve language/region/source metadata.
  - Normalize common save, bug, location, map, quest, and settings terms into discovery hints.
- 测试任务:
  - Mock JP and KR payloads using Unicode strings.
  - Verify normalized records keep `source_region=jp` and `source_region=kr`.
  - Verify multilingual hints work without translation API or LLM.
  - Verify no full post content is retained.
- 协作验收任务:
  - Worker summary must include one JP and one KR normalized example.

允许修改:

- `packages/collectors/*`
- `packages/discovery/*`
- `tests/test_discovery_search.py`
- `docs/player_pain_discovery.md`
- `.piko/summaries/worker_RM-2-R03.md`
- `.piko/summaries/worker_RM-2.md`
- `.piko/round_status.json`

禁止修改:

- Do not add translation API calls.
- Do not call LLM for language classification.
- Do not scrape complete JP/KR posts.

必须运行的验证:

- `python -m pytest tests\test_discovery_search.py -q`
- `python -m pytest`

完成定义:

- RM-2 has Steam, Reddit, SERP, JP, and KR connector contracts tested with mocked payloads.
- Stage summary `.piko/summaries/worker_RM-2.md` exists.
- Status is ready for Piko-verify with `next_round=RM-3-R01`.

输出格式:
- 修改了什么
- 每个任务状态
- 验证结果
- 协作验收结果
- 未完成/风险
- 下一轮建议
