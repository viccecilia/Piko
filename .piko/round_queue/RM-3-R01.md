# Round ID: RM-3-R01

Round Name: Real Market Hot Game Ranking

本轮目标:

Rank current hot games from normalized real-market source records.

本轮任务:
- 执行任务:
  - Add ranking logic that combines player rank, review velocity, discussion velocity, update recency, cross-region mentions, and source diversity.
  - Produce Top 5 and Top 20 hot game outputs.
  - Preserve whether rankings came from fixture mode or real-source mode.
- 测试任务:
  - Mock multi-source hot game records and verify stable ranking.
  - Test ranking prefers recent high-velocity games over old low-velocity games.
  - Test output remains bounded and serializable.
- 协作验收任务:
  - Worker summary must include a sample Top 5 table.

允许修改:

- `packages/discovery/*`
- `apps/api/routes/discovery.py`
- `tests/test_discovery_search.py`
- `.piko/summaries/worker_RM-3-R01.md`
- `.piko/round_status.json`

禁止修改:

- Do not require live endpoints for default tests.
- Do not publish ranking results externally.

必须运行的验证:

- `python -m pytest tests\test_discovery_search.py -q`

完成定义:

- Piko can compute hot game Top 5/Top 20 from normalized source data.

输出格式:
- 修改了什么
- 每个任务状态
- 验证结果
- 协作验收结果
- 未完成/风险
- 下一轮建议
