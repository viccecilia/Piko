# Round ID: RM-3-R02

Round Name: Hot Question And Guide Need Ranking

本轮目标:

Rank player questions by guide need, answer maturity, watchlist value, conflict value, and risk.

本轮任务:
- 执行任务:
  - Add ranking outputs for:
    - hot answered questions
    - hot unanswered questions
    - conflict answer topics
    - high-risk blocked topics
    - must-check-guide topics
  - Include decision, intent, evidence quality, heat, answer status, risk, and recommended next action.
- 测试任务:
  - Mock mixed answered/unanswered/conflict/high-risk topics and verify each ranking bucket.
  - Test high-risk topics cannot appear as normal publish candidates.
  - Test watchlist topics remain non-runnable until evidence maturity changes.
- 协作验收任务:
  - Worker summary must include sample ranking buckets and one watchlist example.

允许修改:

- `packages/discovery/*`
- `apps/api/routes/discovery.py`
- `tests/test_discovery_search.py`
- `.piko/summaries/worker_RM-3-R02.md`
- `.piko/round_status.json`

禁止修改:

- Do not auto-generate article drafts from ranking alone.
- Do not bypass evidence pipeline.
- Do not mark discovery output as publish-ready.

必须运行的验证:

- `python -m pytest tests\test_discovery_search.py -q`

完成定义:

- Hot player questions are ranked into actionable buckets without becoming publish permissions.

输出格式:
- 修改了什么
- 每个任务状态
- 验证结果
- 协作验收结果
- 未完成/风险
- 下一轮建议
