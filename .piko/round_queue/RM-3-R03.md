# Round ID: RM-3-R03

Round Name: Discovery Client Ranking Surface

本轮目标:

Expose real-market hot game and hot question rankings in the Piko discovery client.

本轮任务:
- 执行任务:
  - Update `/discovery/window` to show:
    - current hot games Top 5
    - game type rankings
    - audience/profile rankings
    - must-check-guide rankings
    - answered vs unanswered hot question buckets
  - Add API endpoint or extend existing endpoint for rankings.
  - Mark whether results are fixture or real-source mode.
- 测试任务:
  - Test ranking API returns required sections.
  - Test discovery window contains ranking labels and endpoint wiring.
  - Test default page load does not trigger real network calls.
- 协作验收任务:
  - Worker summary must include the local URL and the visible ranking sections.

允许修改:

- `apps/api/routes/discovery.py`
- `packages/discovery/*`
- `tests/test_discovery_search.py`
- `.piko/summaries/worker_RM-3-R03.md`
- `.piko/summaries/worker_RM-3.md`
- `.piko/round_status.json`

禁止修改:

- Do not add frontend build systems unless necessary.
- Do not auto-enable live source collection from page load.
- Do not publish or deploy.

必须运行的验证:

- `python -m pytest tests\test_discovery_search.py -q`
- `python -m pytest`

完成定义:

- Client shows hot game and hot question ranking surfaces.
- Stage summary `.piko/summaries/worker_RM-3.md` exists.
- Status is ready for Piko-verify with `next_round=RM-4-R01`.

输出格式:
- 修改了什么
- 每个任务状态
- 验证结果
- 协作验收结果
- 未完成/风险
- 下一轮建议
