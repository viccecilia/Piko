# Round ID: RM-4-R01

Round Name: Controlled Live Market Smoke

本轮目标:

Run a bounded real-market live smoke only when explicitly opted in.

本轮任务:
- 执行任务:
  - Add a live smoke command or test marker for real-market discovery.
  - Require explicit flags and configured endpoints.
  - Limit source count, result count, timeout, and retained fields.
  - Return a clear skip when credentials/endpoints are missing.
- 测试任务:
  - Default test must skip live smoke.
  - Mock live smoke must pass.
  - If live env is present, bounded live smoke may run and write only a short verification summary.
- 协作验收任务:
  - Worker summary must include whether live smoke was skipped or run and why.

允许修改:

- `packages/discovery/*`
- `tests/test_discovery_search.py`
- `docs/*`
- `.piko/summaries/worker_RM-4-R01.md`
- `.piko/round_status.json`

禁止修改:

- Do not require live internet for normal pytest.
- Do not store full live response bodies.
- Do not run unbounded collection.

必须运行的验证:

- `python -m pytest tests\test_discovery_search.py -q`
- Optional only when configured: explicit live smoke command with opt-in env vars.

完成定义:

- Live smoke is available, bounded, and skipped by default.

输出格式:
- 修改了什么
- 每个任务状态
- 验证结果
- 协作验收结果
- 未完成/风险
- 下一轮建议
