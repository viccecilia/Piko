# Round ID: RM-4-R03

Round Name: Final Real Market Verification And Docs

本轮目标:

Close the real-market discovery implementation with final tests, docs, and operator instructions.

本轮任务:
- 执行任务:
  - Update docs explaining:
    - how to configure real-market endpoints
    - how to run fixture mode
    - how to run bounded live smoke
    - how to read ranking outputs
    - how watchlist and publish candidates differ
  - Add final batch summary for RM-1 through RM-4.
  - Ensure `/discovery/window` clearly marks fixture vs real-source mode.
- 测试任务:
  - Run full pytest.
  - Run discovery CLI/API smoke.
  - Run article pipeline smoke.
  - Run safety scans for crawler, raw source, publish, deploy, default LLM, and gate bypass.
- 协作验收任务:
  - Worker summary must include final readiness statement and remaining limitations.

允许修改:

- `docs/*`
- `apps/api/routes/discovery.py`
- `packages/discovery/*`
- `tests/test_discovery_search.py`
- `.piko/summaries/worker_RM-4-R03.md`
- `.piko/summaries/worker_RM-4.md`
- `.piko/summaries/worker_real_market_discovery_batch.md`
- `.piko/round_status.json`

禁止修改:

- Do not publish or deploy.
- Do not remove tests.
- Do not bypass verification.
- Do not claim broad live coverage if endpoints are not configured or live smoke was skipped.

必须运行的验证:

- `python -m pytest`
- `python -m pytest tests\test_discovery_search.py -q`
- `python -m packages.discovery.search_cli --min-game-heat 50 --limit 5`
- `python -m packages.workflows.article_pipeline`

完成定义:

- RM-1 through RM-4 are complete.
- Final batch summary exists.
- Status is ready for Piko-verify with `next_round` set to the next operator-selected queue or `null`.
- Piko can perform controlled real-market discovery when endpoints and opt-in flags are configured.

输出格式:
- 修改了什么
- 每个任务状态
- 验证结果
- 协作验收结果
- 未完成/风险
- 下一轮建议
