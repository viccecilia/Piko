# Round ID: TD-0-R01

Round Name: Topic Discovery Baseline Inventory

本轮目标:
Record the current player pain discovery capabilities and gaps before strengthening topic search.

本轮任务:
- 执行任务:
  - Read `docs/player_pain_discovery.md`, `packages/discovery/*`, `fixtures/player_questions/*`, and `tests/test_discovery_search.py`.
  - Document the current baseline: fixture-backed scoring, clusters, decisions, API, CLI, and DA candidate handoff.
- 测试任务:
  - Run discovery tests and CLI smoke.
- 协作验收任务:
  - Worker summary must state why DA-3 is paused until TD is complete.

允许修改:
- `docs/player_pain_discovery.md`
- `.piko/summaries/worker_TD-0-R01.md`
- `.piko/round_status.json`

禁止修改:
- Do not modify runtime code.
- Do not add live collection.
- Do not publish or deploy.

必须运行的验证:
- `python -m pytest tests\test_discovery_search.py -q`
- `python -m packages.discovery.search_cli --min-game-heat 50 --limit 5`

完成定义:
- Baseline is documented and tests pass.

输出格式:
- 修改了什么
- 每个任务状态
- 验证结果
- 协作验收结果
- 未完成/风险
- 下一轮建议
