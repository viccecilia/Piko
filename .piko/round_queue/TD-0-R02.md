# Round ID: TD-0-R02

Round Name: Topic Discovery Queue Readiness

本轮目标:
Confirm TD round files and stage-batch execution are ready.

本轮任务:
- 执行任务:
  - Check `.piko/round_queue/TD-INDEX.md` and all `TD-*.md` files exist.
  - Confirm `.piko/round_status.json` points to a valid TD round.
- 测试任务:
  - Run discovery tests.
- 协作验收任务:
  - Worker summary must list the next executable TD round.

允许修改:
- `.piko/round_queue/TD-*.md`
- `.piko/summaries/worker_TD-0-R02.md`
- `.piko/round_status.json`

禁止修改:
- Do not change runtime behavior.

必须运行的验证:
- `python -m pytest tests\test_discovery_search.py -q`

完成定义:
- TD queue is complete and status points to a valid TD round.

输出格式:
- 修改了什么
- 每个任务状态
- 验证结果
- 协作验收结果
- 未完成/风险
- 下一轮建议
