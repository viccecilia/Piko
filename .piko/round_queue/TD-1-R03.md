# Round ID: TD-1-R03

Round Name: Topic Actionability Classifier

本轮目标:
Judge whether a topic can become one useful Piko page.

本轮任务:
- 执行任务:
  - Add actionability labels such as `single_page_answerable`, `needs_more_sources`, `too_broad`, `too_risky`, and `too_visual`.
  - Ensure map/image-heavy topics are marked carefully.
- 测试任务:
  - Test save-location, crash, broad build, risky tool, and map exploration cases.
- 协作验收任务:
  - Worker summary must explain which topics should not enter article generation.

允许修改:
- `packages/shared/schemas.py`
- `packages/discovery/scoring.py`
- `packages/discovery/search_engine.py`
- `fixtures/player_questions/*`
- `tests/test_discovery_search.py`
- `.piko/summaries/worker_TD-1-R03.md`
- `.piko/round_status.json`

禁止修改:
- Do not generate article drafts in this round.
- Do not copy map/image/table material.

必须运行的验证:
- `python -m pytest tests\test_discovery_search.py -q`

完成定义:
- Topic actionability is explicit and tested.

输出格式:
- 修改了什么
- 每个任务状态
- 验证结果
- 协作验收结果
- 未完成/风险
- 下一轮建议
