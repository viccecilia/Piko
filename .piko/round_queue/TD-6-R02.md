# Round ID: TD-6-R02

Round Name: Topic Search CLI Upgrade

本轮目标:
Make CLI useful for operator topic triage.

本轮任务:
- 执行任务:
  - Add CLI flags for decision, intent/lifecycle/actionability if implemented.
  - Add optional table/summary output while preserving JSON.
- 测试任务:
  - Run CLI smoke for publish candidates and watchlist topics.
- 协作验收任务:
  - Worker summary must include CLI examples.

允许修改:
- `packages/discovery/search_cli.py`
- `tests/test_discovery_search.py`
- `docs/player_pain_discovery.md`
- `.piko/summaries/worker_TD-6-R02.md`
- `.piko/round_status.json`

禁止修改:
- Do not require frontend or network.

必须运行的验证:
- `python -m packages.discovery.search_cli --min-game-heat 50 --limit 5`
- `python -m pytest tests\test_discovery_search.py -q`

完成定义:
- CLI supports stronger triage without losing JSON output.

输出格式:
- 修改了什么
- 每个任务状态
- 验证结果
- 协作验收结果
- 未完成/风险
- 下一轮建议
