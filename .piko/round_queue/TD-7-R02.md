# Round ID: TD-7-R02

Round Name: Topic Discovery Live Smoke Contract

本轮目标:
Prepare a bounded live-smoke contract for the selected real source.

本轮任务:
- 执行任务:
  - Define env flags, timeout, result limit, retained fields, and skip behavior.
  - Implement only if it can be done with mock/default-offline tests.
- 测试任务:
  - Normal pytest must not touch network.
  - Live smoke must skip unless explicitly opted in.
- 协作验收任务:
  - Worker summary must state whether live smoke was implemented or only specified.

允许修改:
- `packages/collectors/*`
- `packages/discovery/*`
- `packages/shared/config.py`
- `tests/test_discovery_search.py`
- `docs/player_pain_discovery.md`
- `.piko/summaries/worker_TD-7-R02.md`
- `.piko/round_status.json`

禁止修改:
- Do not default-enable live collection.
- Do not crawl pages.
- Do not store full posts.

必须运行的验证:
- `python -m pytest tests\test_discovery_search.py -q`
- `python -m pytest`

完成定义:
- Real-source live smoke is bounded, opt-in, or clearly planned.

输出格式:
- 修改了什么
- 每个任务状态
- 验证结果
- 协作验收结果
- 未完成/风险
- 下一轮建议
