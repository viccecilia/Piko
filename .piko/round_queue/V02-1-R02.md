# Round ID: V02-1-R02

Round Name: Draft To Round Materializer Dry Run

本轮目标:

实现 draft-to-round materializer 的 dry-run 模式，只生成预览，不写正式执行队列。

本轮任务:
- 执行任务:
  - 读取 `artifacts/growth_loop/latest_draft_queue_package.json`。
  - 读取 approval packet；无批准时只能输出 dry-run preview。
  - 生成 `artifacts/v02_runtime/latest_materialization_preview.json`。
  - preview 必须显示将创建哪些 worker/verify 文件，但不得实际写入 `.piko/round_queue`。
- 测试任务:
  - 测试无批准时 `materialization_performed=false`。
  - 测试 preview JSON 可解析。
  - 测试 `.piko/round_queue` 没有新增 auto-generated executable rounds。
- 协作验收任务:
  - 生成 `.piko/summaries/worker_V02-1-R02.md`。
  - 生成 `.piko/summaries/worker_V02-1.md`。

允许修改:

- `packages/*`
- `tests/*`
- `docs/*`
- `artifacts/v02_runtime/*`
- `.piko/summaries/worker_V02-1-R02.md`
- `.piko/summaries/worker_V02-1.md`
- `.piko/round_status.json`

禁止修改:

- 不要写正式 round 文件。
- 不要自动执行 materialized queue。

必须运行的验证:

- Materialization dry-run tests。
- No executable round generation probe。

完成定义:

- 可预览 draft 转正式任务的结果，但默认不执行。

输出格式:
- 修改了什么
- 每个任务状态
- 验证结果
- 协作验收结果
- 未完成/风险
- 下一轮建议
