# Round ID: V02-2-R01

Round Name: DomainPlugin Runtime Contract

本轮目标:

把之前的 DomainPlugin proposal 变成最小 runtime contract，但保持 demo/fixture 优先。

本轮任务:
- 执行任务:
  - 定义 `DomainPlugin` runtime contract。
  - 字段至少包含 `domain_id`、`status`、`source_contracts`、`topic_scoring`、`evidence_rules`、`writer_profile`、`verification_rules`、`publish_policy`。
  - 默认 active domain 仍是 `gaming`。
  - 新 domain 必须默认 `enabled_by_default=false`。
- 测试任务:
  - gaming 默认行为不回归。
  - unknown domain 安全失败。
  - demo domain 不触网、不发布。
- 协作验收任务:
  - 生成 `.piko/summaries/worker_V02-2-R01.md`。

允许修改:

- `packages/*`
- `apps/api/routes/*`
- `tests/*`
- `docs/*`
- `artifacts/v02_runtime/*`
- `.piko/summaries/worker_V02-2-R01.md`
- `.piko/round_status.json`

禁止修改:

- 不要默认启用新 domain。
- 不要破坏 gaming discovery。

必须运行的验证:

- DomainPlugin contract tests。
- `python -m pytest tests\test_discovery_search.py -q`

完成定义:

- 最小 DomainPlugin runtime contract 存在且默认安全。

输出格式:
- 修改了什么
- 每个任务状态
- 验证结果
- 协作验收结果
- 未完成/风险
- 下一轮建议
