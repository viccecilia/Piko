# Round ID: V02-2-R02

Round Name: AI Tools Demo Domain Fixture

本轮目标:

实现一个非游戏 demo domain fixture，用于证明 Piko 的可插拔方向，但不触发真实联网。

本轮任务:
- 执行任务:
  - 新增 `ai_tools` demo domain fixture。
  - fixture 可包含开源工具问题：安装、对比、替代方案、使用场景、风险。
  - 输出 demo candidate/result 时必须 `candidate_only=true`、`publish_ready=false`、`real_collection_performed=false`。
- 测试任务:
  - `ai_tools` fixture probe 正常。
  - demo domain 不触网、不调用 LLM。
  - demo domain 不进入 public publish flow。
- 协作验收任务:
  - 生成 `.piko/summaries/worker_V02-2-R02.md`。

允许修改:

- `packages/*`
- `fixtures/*`
- `apps/api/routes/*`
- `tests/*`
- `docs/*`
- `.piko/summaries/worker_V02-2-R02.md`
- `.piko/round_status.json`

禁止修改:

- 不要真实采集 GitHub/网页。
- 不要发布 demo 内容。

必须运行的验证:

- AI tools domain fixture tests。

完成定义:

- Piko 有一个非游戏 demo domain fixture，证明可插拔方向。

输出格式:
- 修改了什么
- 每个任务状态
- 验证结果
- 协作验收结果
- 未完成/风险
- 下一轮建议
