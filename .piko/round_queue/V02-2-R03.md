# Round ID: V02-2-R03

Round Name: Domain Plugin Routing Surface

本轮目标:

提供只读 domain routing surface，让操作员看到当前 domain 插件状态和路由结果。

本轮任务:
- 执行任务:
  - 新增或更新 `/domains`、`/domains/window` 或现有等价 endpoint。
  - 展示 gaming、ai_tools、unknown domain 的安全路由状态。
  - 输出必须含 `candidate_only=true`、`publish_ready=false`。
- 测试任务:
  - API/window probe。
  - unknown domain 安全失败。
  - 页面不引用外部 CDN。
- 协作验收任务:
  - 生成 `.piko/summaries/worker_V02-2-R03.md`。
  - 生成 `.piko/summaries/worker_V02-2.md`。

允许修改:

- `apps/api/routes/*`
- `packages/*`
- `tests/*`
- `docs/*`
- `.piko/summaries/worker_V02-2-R03.md`
- `.piko/summaries/worker_V02-2.md`
- `.piko/round_status.json`

禁止修改:

- 不要加入执行按钮。
- 不要自动切换 active domain。

必须运行的验证:

- Domain routing API/window probes。

完成定义:

- Domain 插件状态可观察，路由安全。

输出格式:
- 修改了什么
- 每个任务状态
- 验证结果
- 协作验收结果
- 未完成/风险
- 下一轮建议
