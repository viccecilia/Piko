# Round ID: V02-3-R03

Round Name: Framework Candidate Comparison

本轮目标:

用统一标准比较 LangGraph、CrewAI、LlamaIndex、OpenAI Agents SDK 等候选，不做安装。

本轮任务:
- 执行任务:
  - 生成 `artifacts/v02_runtime/framework_candidate_comparison.json`。
  - 比较维度至少包含 workflow_fit、evidence_fit、tool_policy_fit、testability、lock_in_risk、integration_cost、recommended_next_step。
  - 输出推荐只能是 `watch`、`adapter_spike_candidate`、`reject_for_now`。
- 测试任务:
  - JSON parse probe。
  - 不得出现 `installed=true`。
  - 不得出现 `active_runtime_replaced=true`。
- 协作验收任务:
  - 生成 `.piko/summaries/worker_V02-3-R03.md`。
  - 生成 `.piko/summaries/worker_V02-3.md`。

允许修改:

- `packages/*`
- `tests/*`
- `docs/*`
- `artifacts/v02_runtime/*`
- `.piko/summaries/worker_V02-3-R03.md`
- `.piko/summaries/worker_V02-3.md`
- `.piko/round_status.json`

禁止修改:

- 不要安装或启用外部框架。
- 不要提交依赖变更。

必须运行的验证:

- Framework comparison JSON parse probe。
- No install/no runtime replacement scan。

完成定义:

- Piko 能理性比较成熟 agent 框架，并保持候选状态。

输出格式:
- 修改了什么
- 每个任务状态
- 验证结果
- 协作验收结果
- 未完成/风险
- 下一轮建议
