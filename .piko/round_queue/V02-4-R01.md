# Round ID: V02-4-R01

Round Name: Declarative Eval Pack Contract

本轮目标:

建立 Piko eval packs 合同，用声明式方式验证事实、来源、风险和 Gate。

本轮任务:
- 执行任务:
  - 定义 `artifacts/v02_runtime/eval_pack_contract.json`。
  - 覆盖 claim-source trace、source freshness、risk wording、unsupported claims、publish safety、adapter boundary。
  - 输出示例 eval pack。
- 测试任务:
  - JSON parse probe。
  - eval pack 至少包含 5 类检查。
  - eval 不能替代 evidence source IDs。
- 协作验收任务:
  - 生成 `.piko/summaries/worker_V02-4-R01.md`。

允许修改:

- `packages/*`
- `tests/*`
- `docs/*`
- `artifacts/v02_runtime/*`
- `.piko/summaries/worker_V02-4-R01.md`
- `.piko/round_status.json`

禁止修改:

- 不要引入外部 eval service。
- 不要放宽现有 verification。

必须运行的验证:

- Eval pack contract tests。

完成定义:

- Piko 有可扩展 eval pack 合同。

输出格式:
- 修改了什么
- 每个任务状态
- 验证结果
- 协作验收结果
- 未完成/风险
- 下一轮建议
