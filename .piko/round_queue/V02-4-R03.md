# Round ID: V02-4-R03

Round Name: Operator Trace Window

本轮目标:

把 eval pack 和 run trace 展示到只读 operator window，方便追踪 Piko 成长和失败原因。

本轮任务:
- 执行任务:
  - 新增或更新 `/operator/trace-window` 或等价 endpoint。
  - 展示 run status、Gate failures、commands、artifact refs、human approval state。
  - 页面必须只读，不提供执行按钮。
- 测试任务:
  - window probe 返回 200。
  - 页面包含 Trace、Gate、Verification、Human approval。
  - 页面不引用外部 CDN。
- 协作验收任务:
  - 生成 `.piko/summaries/worker_V02-4-R03.md`。
  - 生成 `.piko/summaries/worker_V02-4.md`。

允许修改:

- `apps/api/routes/*`
- `packages/*`
- `tests/*`
- `docs/*`
- `.piko/summaries/worker_V02-4-R03.md`
- `.piko/summaries/worker_V02-4.md`
- `.piko/round_status.json`

禁止修改:

- 不要加入执行按钮。
- 不要发布、部署。

必须运行的验证:

- Operator trace window probe。
- HTML external resource scan。

完成定义:

- Operator 可以看到本地 trace 和 eval 状态。

输出格式:
- 修改了什么
- 每个任务状态
- 验证结果
- 协作验收结果
- 未完成/风险
- 下一轮建议
