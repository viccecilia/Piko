# Round ID: V02-5-R01

Round Name: Controlled Real Pilot Readiness

本轮目标:

准备一个受控真实试点 readiness 检查，但不实际联网，除非已有 approved endpoint 和双 opt-in。

本轮任务:
- 执行任务:
  - 生成 `artifacts/v02_runtime/real_pilot_readiness.json`。
  - 检查 approved endpoint、double opt-in、payload limit、artifact safety、publish disabled、verification required。
  - 如果缺 endpoint，状态必须是 `blocked_for_endpoint` 或 `not_ready`，不能伪装成功。
- 测试任务:
  - 无 endpoint 时安全 blocked/skipped。
  - 有 mock approved endpoint 时只跑 fixture/mock。
  - 不保存 raw response body。
- 协作验收任务:
  - 生成 `.piko/summaries/worker_V02-5-R01.md`。

允许修改:

- `packages/*`
- `tests/*`
- `docs/*`
- `artifacts/v02_runtime/*`
- `.piko/summaries/worker_V02-5-R01.md`
- `.piko/round_status.json`

禁止修改:

- 不要真实联网，除非用户明确配置 approved endpoint 和双 opt-in。
- 不要发布。

必须运行的验证:

- Real pilot readiness tests。

完成定义:

- Piko 知道是否具备真实试点条件，且不会伪装 live success。

输出格式:
- 修改了什么
- 每个任务状态
- 验证结果
- 协作验收结果
- 未完成/风险
- 下一轮建议
