# Round ID: V03-1-R01

Round Name: First Practical Plugin Candidate

本轮目标:

确定 V03 的第一个实战型成熟能力候选，并把选择理由、边界、预期收益写成可验证 artifact。默认方向应优先考虑 LangGraph-style workflow adapter，除非本地证据显示另一个候选明显更快服务 Piko。

本轮任务:
- 执行任务:
  - 阅读 V02 runtime artifacts，确认现有 AgentRuntimeAdapter / DomainPlugin 基础。
  - 建立 practical candidate selection artifact，至少比较 LangGraph-style workflow、LlamaIndex-style evidence/RAG、promptfoo-style eval 三类候选。
  - 给出 first candidate、why now、expected Piko workflow benefit、activation boundary。
- 测试任务:
  - 增加或更新测试，确认候选选择 artifact 可解析，且不是 auto-activation。
  - 确认 candidate_only=true、auto_install_allowed=false、active_replacement_allowed=false。
- 协作验收任务:
  - 生成 `.piko/summaries/worker_V03-1-R01.md`。

允许修改:

- `packages/*`
- `tests/*`
- `docs/*`
- `artifacts/v03_practical_plugin/*`
- `.piko/summaries/worker_V03-1-R01.md`
- `.piko/round_status.json`

禁止修改:

- 不安装外部依赖。
- 不 vendor 外部仓库源码。
- 不替换 active capability。

必须运行的验证:

- Candidate artifact JSON parse probe
- Relevant unit tests

完成定义:

- 明确选出 first practical candidate。
- 选择理由服务“尽快实战”，不是泛泛研究。
- Artifact 明确保持 candidate/dry-run。

输出格式:
- 修改了什么
- 每个任务状态
- 验证结果
- 协作验收结果
- 未完成/风险
- 下一轮建议
