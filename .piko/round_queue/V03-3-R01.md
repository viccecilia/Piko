# Round ID: V03-3-R01

Round Name: Discovery Workflow Through Adapter

本轮目标:

把一个真实 Piko discovery 场景接到 adapter fixture 中，证明它能服务当前产品，而不是孤立 demo。

本轮任务:
- 执行任务:
  - 使用现有 fixture/candidate 数据，跑 discovery candidate -> evidence/ranking -> decision handoff。
  - 输出 workflow_result artifact，包含 selected topic、evidence summary、ranking decision、trace ids。
  - 保持 publish_ready=false。
- 测试任务:
  - 测试结果可追溯到 candidate/evidence/ranking。
  - 测试 watchlist/high-risk 不进入 normal publish candidate。
- 协作验收任务:
  - 生成 `.piko/summaries/worker_V03-3-R01.md`。

允许修改:

- `packages/*`
- `tests/*`
- `artifacts/v03_practical_plugin/*`
- `.piko/summaries/worker_V03-3-R01.md`
- `.piko/round_status.json`

禁止修改:

- 不触发真实采集。
- 不生成 public publish approval。

必须运行的验证:

- Discovery workflow adapter tests

完成定义:

- Adapter fixture 跑通一个 Piko discovery workflow。
- 输出是候选/内部结果，不是发布许可。

输出格式:
- 修改了什么
- 每个任务状态
- 验证结果
- 协作验收结果
- 未完成/风险
- 下一轮建议
