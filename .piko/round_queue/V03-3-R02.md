# Round ID: V03-3-R02

Round Name: Article Package Handoff Without Publish

本轮目标:

把 adapter workflow 的结果交给 article package handoff，但保持 internal draft only，证明 V03 可以推动实战内容生产但不越过发布边界。

本轮任务:
- 执行任务:
  - 从 workflow_result 生成 article_package_handoff artifact。
  - Artifact 必须包含 source/evidence trace、draft intent、verification required、publish_ready=false、publishing_performed=false。
  - 不要求写完整文章，只要求形成可交给 article pipeline 的安全包。
- 测试任务:
  - 测试 handoff artifact 字段完整。
  - 测试无 publish/deploy side effects。
- 协作验收任务:
  - 生成 `.piko/summaries/worker_V03-3-R02.md` 和 `.piko/summaries/worker_V03-3.md`。

允许修改:

- `packages/*`
- `tests/*`
- `artifacts/v03_practical_plugin/*`
- `.piko/summaries/worker_V03-3-R02.md`
- `.piko/summaries/worker_V03-3.md`
- `.piko/round_status.json`

禁止修改:

- 不发布。
- 不部署。
- 不绕过 article verification。

必须运行的验证:

- Article handoff safety tests
- Existing article_pipeline smoke if relevant

完成定义:

- V03 workflow 能产生可用于内容生产的安全 handoff。
- 发布仍被阻断。

输出格式:
- 修改了什么
- 每个任务状态
- 验证结果
- 协作验收结果
- 未完成/风险
- 下一轮建议
