# Round ID: V03-4-R01

Round Name: Operator Trace Surface

本轮目标:

让 operator 能看到 adapter workflow 的每一步动作：搜什么、怎么筛、怎么排名、在哪里 Gate、为什么不能发布。

本轮任务:
- 执行任务:
  - 新增或扩展 operator trace artifact/API/window。
  - 展示 node timeline、state summary、Gate decisions、retry/failure summary、safety flags。
  - 页面或 API 必须只读，不执行 workflow。
- 测试任务:
  - API/window probe 如果实现。
  - 测试页面无外部 URL/CDN。
- 协作验收任务:
  - 生成 `.piko/summaries/worker_V03-4-R01.md`。

允许修改:

- `apps/*`
- `packages/*`
- `tests/*`
- `docs/*`
- `artifacts/v03_practical_plugin/*`
- `.piko/summaries/worker_V03-4-R01.md`
- `.piko/round_status.json`

禁止修改:

- 不让窗口按钮执行真实发布/部署。
- 不引用外部 CDN。

必须运行的验证:

- Operator trace surface tests
- HTML external URL guardrail if HTML generated

完成定义:

- Operator 能看懂 V03 adapter workflow 如何运行。
- Trace surface 不产生副作用。

输出格式:
- 修改了什么
- 每个任务状态
- 验证结果
- 协作验收结果
- 未完成/风险
- 下一轮建议
