# Round ID: V04-2-R02

Round Name: Controlled Install Path Or Safe Block

本轮目标:

如果 approval 明确允许安装，则执行受控安装/记录；否则保持 safe block。目标是实战推进，但不能越权。

本轮任务:
- 执行任务:
  - 读取 approval artifact 的 install_allowed 和 approved_commands。
  - 若 install_allowed=false，生成 blocked_for_approval artifact。
  - 若 install_allowed=true，使用 approved command 执行最小安装或说明本地已可用。
  - 记录 command、exit status、version、rollback note。
- 测试任务:
  - 测试未批准安装不会执行安装路径。
  - 测试 artifact 明确 install_performed true/false。
- 协作验收任务:
  - 生成 `.piko/summaries/worker_V04-2-R02.md`。

允许修改:

- `packages/*`
- `tests/*`
- `artifacts/v04_langgraph_backend/*`
- `.piko/summaries/worker_V04-2-R02.md`
- `.piko/round_status.json`

禁止修改:

- 不使用未批准命令。
- 不把失败安装写成成功。

必须运行的验证:

- Install path / safe block tests

完成定义:

- 安装路径被批准则受控执行，否则正确阻断。
- local_fixture 不受影响。

输出格式:
- 修改了什么
- 每个任务状态
- 验证结果
- 协作验收结果
- 未完成/风险
- 下一轮建议
