# Round ID: V04-3-R01

Round Name: Backend Selector Contract

本轮目标:

让 V03 adapter 支持 backend selector：`local_fixture` 与 `langgraph_backend`。默认必须是 local_fixture。

本轮任务:
- 执行任务:
  - 定义 backend selector contract/artifact。
  - 默认 backend=local_fixture。
  - langgraph_backend 只有在 approval + availability + explicit request 时可选。
- 测试任务:
  - 测试默认选择 local_fixture。
  - 测试未批准时选择 langgraph_backend 会被阻断。
  - 测试 selector 不替换 active runtime。
- 协作验收任务:
  - 生成 `.piko/summaries/worker_V04-3-R01.md`。

允许修改:

- `packages/*`
- `tests/*`
- `artifacts/v04_langgraph_backend/*`
- `.piko/summaries/worker_V04-3-R01.md`
- `.piko/round_status.json`

禁止修改:

- 不让 langgraph_backend 成为默认 backend。
- 不改 production config。

必须运行的验证:

- Backend selector tests

完成定义:

- backend 选择可控、可测、默认安全。

输出格式:
- 修改了什么
- 每个任务状态
- 验证结果
- 协作验收结果
- 未完成/风险
- 下一轮建议
