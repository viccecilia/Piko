# Round ID: V04-3-R02

Round Name: LangGraph Backend Adapter Shape

本轮目标:

实现或准备 LangGraph backend adapter shape。若真实依赖不可用，也要保留 contract-compatible stub，并明确 blocked 状态。

本轮任务:
- 执行任务:
  - 创建 backend adapter module/contract。
  - 如果 LangGraph 可用，提供最小 graph build/run smoke。
  - 如果不可用，提供 blocked stub，不抛不可控异常。
  - 输出 adapter artifact。
- 测试任务:
  - 测试可用路径或 blocked stub。
  - 测试不调用 LLM/外部 connector。
- 协作验收任务:
  - 生成 `.piko/summaries/worker_V04-3-R02.md` 和 `.piko/summaries/worker_V04-3.md`。

允许修改:

- `packages/*`
- `tests/*`
- `artifacts/v04_langgraph_backend/*`
- `.piko/summaries/worker_V04-3-R02.md`
- `.piko/summaries/worker_V04-3.md`
- `.piko/round_status.json`

禁止修改:

- 不引入不受控 runtime side effects。
- 不隐藏 import failure。

必须运行的验证:

- Backend adapter tests

完成定义:

- Piko 具有真实 backend adapter shape。
- 不可用时失败是可解释、可验证的。

输出格式:
- 修改了什么
- 每个任务状态
- 验证结果
- 协作验收结果
- 未完成/风险
- 下一轮建议
