# Round ID: V04-4-R01

Round Name: LangGraph Backend Smoke Workflow

本轮目标:

在 selector 允许的情况下，用 langgraph_backend 跑 discovery -> evidence/ranking -> draft_handoff -> verification_gate smoke；不可用时必须 blocked 并保留 local fixture smoke。

本轮任务:
- 执行任务:
  - 运行 backend smoke workflow。
  - 输出 smoke trace artifact。
  - 字段包含 requested_backend、effective_backend、backend_status、nodes、gate_decision、publish_ready=false。
- 测试任务:
  - 测试成功路径或 blocked path 都可解析。
  - 测试 publish/deploy side effects false。
- 协作验收任务:
  - 生成 `.piko/summaries/worker_V04-4-R01.md`。

允许修改:

- `packages/*`
- `tests/*`
- `artifacts/v04_langgraph_backend/*`
- `.piko/summaries/worker_V04-4-R01.md`
- `.piko/round_status.json`

禁止修改:

- 不使用真实 external source/LLM。
- 不发布。

必须运行的验证:

- Backend smoke workflow tests

完成定义:

- 同一条 Piko workflow 可通过 selector 尝试 real backend。
- 不可用时结果为 blocked，不伪装成功。

输出格式:
- 修改了什么
- 每个任务状态
- 验证结果
- 协作验收结果
- 未完成/风险
- 下一轮建议
