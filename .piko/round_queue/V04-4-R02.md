# Round ID: V04-4-R02

Round Name: Operator Backend Status Surface

本轮目标:

让 operator 能看到 V04 backend 状态：approval、dependency、install/probe、backend selector、workflow smoke、blocked reason。

本轮任务:
- 执行任务:
  - 新增或扩展 API/window/status artifact。
  - 展示 backend_status、approval_status、install_performed、effective_backend、fallback_available、activation_status。
  - Surface 必须只读。
- 测试任务:
  - API/window probe 如果实现。
  - HTML 无外部 URL/CDN。
- 协作验收任务:
  - 生成 `.piko/summaries/worker_V04-4-R02.md` 和 `.piko/summaries/worker_V04-4.md`。

允许修改:

- `apps/*`
- `packages/*`
- `tests/*`
- `docs/*`
- `artifacts/v04_langgraph_backend/*`
- `.piko/summaries/worker_V04-4-R02.md`
- `.piko/summaries/worker_V04-4.md`
- `.piko/round_status.json`

禁止修改:

- 不让窗口执行安装/发布/部署。
- 不引用外部 CDN。

必须运行的验证:

- Operator backend status tests
- HTML external URL guardrail if HTML generated

完成定义:

- Operator 能清楚判断真实 backend 是 success 还是 blocked。

输出格式:
- 修改了什么
- 每个任务状态
- 验证结果
- 协作验收结果
- 未完成/风险
- 下一轮建议
