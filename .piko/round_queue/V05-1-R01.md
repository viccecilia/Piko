# Round ID: V05-1-R01

Round Name: Explicit LangGraph Install Approval

本轮目标:

生成 V05 显式安装批准 artifact，只允许安装/探测 LangGraph 依赖和本地 smoke，不允许生产启用。

本轮任务:
- 执行任务:
  - 读取 V04 artifacts，确认当前 blocked_for_dependency。
  - 生成 V05 explicit install approval artifact。
  - 字段包含 install_approved、approved_package、approved_command、version_policy、production_activation_allowed=false、rollback_note。
- 测试任务:
  - 测试 approval artifact 可解析。
  - 测试 production_activation_allowed=false。
- 协作验收任务:
  - 生成 `.piko/summaries/worker_V05-1-R01.md`。

允许修改:

- `packages/*`
- `tests/*`
- `docs/*`
- `artifacts/v05_langgraph_install/*`
- `.piko/summaries/worker_V05-1-R01.md`
- `.piko/round_status.json`

禁止修改:

- 不执行安装。
- 不生产启用。

必须运行的验证:

- Approval artifact tests

完成定义:

- V05 有机器可读的显式安装批准。
- 生产启用仍为 false。

输出格式:
- 修改了什么
- 每个任务状态
- 验证结果
- 协作验收结果
- 未完成/风险
- 下一轮建议
