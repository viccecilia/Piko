# Round ID: V05-1-R02

Round Name: Install Command Guardrail

本轮目标:

建立 install command guardrail，确保 worker 只能执行批准命令，不能任意安装包或改写环境。

本轮任务:
- 执行任务:
  - 定义 install guardrail artifact/logic。
  - 只允许批准的 package 和 command pattern。
  - 记录 disallowed command 的 blocked result。
- 测试任务:
  - 测试未批准命令被拒绝。
  - 测试 secrets/env dump 不会写入 artifact。
- 协作验收任务:
  - 生成 `.piko/summaries/worker_V05-1-R02.md` 和 `.piko/summaries/worker_V05-1.md`。

允许修改:

- `packages/*`
- `tests/*`
- `artifacts/v05_langgraph_install/*`
- `.piko/summaries/worker_V05-1-R02.md`
- `.piko/summaries/worker_V05-1.md`
- `.piko/round_status.json`

禁止修改:

- 不执行未批准命令。
- 不保存环境变量全文。

必须运行的验证:

- Install guardrail tests

完成定义:

- 安装命令被严格约束。

输出格式:
- 修改了什么
- 每个任务状态
- 验证结果
- 协作验收结果
- 未完成/风险
- 下一轮建议
