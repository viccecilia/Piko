# Round ID: V05-2-R01

Round Name: Controlled LangGraph Install

本轮目标:

在 V05 approval 和 guardrail 允许时，受控安装或确认 LangGraph 已安装，并记录结果。

本轮任务:
- 执行任务:
  - 读取 approval 和 guardrail。
  - 若 install_approved=false，输出 blocked_for_approval。
  - 若已安装，记录 already_available。
  - 若未安装，执行批准命令并记录 exit status。
- 测试任务:
  - 测试 install result artifact 可解析。
  - 测试失败不会被标记为 success。
- 协作验收任务:
  - 生成 `.piko/summaries/worker_V05-2-R01.md`。

允许修改:

- `packages/*`
- `tests/*`
- `artifacts/v05_langgraph_install/*`
- `.piko/summaries/worker_V05-2-R01.md`
- `.piko/round_status.json`

禁止修改:

- 不 vendor 源码。
- 不修改 active runtime。

必须运行的验证:

- Controlled install tests or blocked status tests

完成定义:

- LangGraph 安装状态被真实记录。

输出格式:
- 修改了什么
- 每个任务状态
- 验证结果
- 协作验收结果
- 未完成/风险
- 下一轮建议
