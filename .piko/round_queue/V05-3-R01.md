# Round ID: V05-3-R01

Round Name: Minimal LangGraph Graph Smoke

本轮目标:

如果 LangGraph import 成功，运行最小 graph smoke；如果不可用，输出 blocked_for_dependency。

本轮任务:
- 执行任务:
  - 构建最小 graph：start -> transform -> gate -> end。
  - 输入固定 fixture state。
  - 输出 graph smoke trace artifact。
- 测试任务:
  - 测试 success trace 或 blocked trace 可解析。
  - 测试 smoke 不调用 LLM/外部 API。
- 协作验收任务:
  - 生成 `.piko/summaries/worker_V05-3-R01.md`。

允许修改:

- `packages/*`
- `tests/*`
- `artifacts/v05_langgraph_install/*`
- `.piko/summaries/worker_V05-3-R01.md`
- `.piko/round_status.json`

禁止修改:

- 不调用真实外部服务。

必须运行的验证:

- Minimal graph smoke tests

完成定义:

- 最小 graph smoke 成功或正确 blocked。

输出格式:
- 修改了什么
- 每个任务状态
- 验证结果
- 协作验收结果
- 未完成/风险
- 下一轮建议
