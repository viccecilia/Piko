# Round ID: V05-4-R01

Round Name: Piko Workflow On LangGraph Backend

本轮目标:

用 LangGraph backend 尝试运行 Piko 同一条 workflow：source_discovery -> evidence_rank -> draft_handoff -> verification_gate。

本轮任务:
- 执行任务:
  - requested_backend=langgraph_backend。
  - 如果 LangGraph 可用，运行 backend workflow smoke。
  - 如果不可用，输出 blocked 并证明 local_fixture 可用。
  - 输出 workflow backend smoke artifact。
- 测试任务:
  - 测试 success/blocked trace 可解析。
  - 测试 verification_required=true、publish_ready=false、publishing_performed=false。
- 协作验收任务:
  - 生成 `.piko/summaries/worker_V05-4-R01.md`。

允许修改:

- `packages/*`
- `tests/*`
- `artifacts/v05_langgraph_install/*`
- `.piko/summaries/worker_V05-4-R01.md`
- `.piko/round_status.json`

禁止修改:

- 不调用真实 data endpoint。
- 不发布。

必须运行的验证:

- Piko backend workflow smoke tests

完成定义:

- Piko workflow 成功跑在 LangGraph backend，或正确 blocked 并 fallback。

输出格式:
- 修改了什么
- 每个任务状态
- 验证结果
- 协作验收结果
- 未完成/风险
- 下一轮建议
