# Round ID: V05-4-R02

Round Name: Operator LangGraph Status Surface

本轮目标:

让 operator 能看到 V05 的安装、版本、smoke 和 workflow backend 状态。

本轮任务:
- 执行任务:
  - 新增或扩展 V05 status API/window/artifact。
  - 展示 install_status、import_success、version、minimal_smoke_status、workflow_smoke_status、effective_backend、fallback_available。
  - Surface 必须只读。
- 测试任务:
  - API/window probe 如果实现。
  - HTML 无外部 URL/CDN。
- 协作验收任务:
  - 生成 `.piko/summaries/worker_V05-4-R02.md` 和 `.piko/summaries/worker_V05-4.md`。

允许修改:

- `apps/*`
- `packages/*`
- `tests/*`
- `docs/*`
- `artifacts/v05_langgraph_install/*`
- `.piko/summaries/worker_V05-4-R02.md`
- `.piko/summaries/worker_V05-4.md`
- `.piko/round_status.json`

禁止修改:

- 不让窗口执行安装/发布/部署。
- 不引用外部 CDN。

必须运行的验证:

- Operator status surface tests

完成定义:

- Operator 能清楚看到 LangGraph 是 success 还是 blocked。

输出格式:
- 修改了什么
- 每个任务状态
- 验证结果
- 协作验收结果
- 未完成/风险
- 下一轮建议
