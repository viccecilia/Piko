# Round ID: V05-5-R01

Round Name: Real Data Handoff Readiness

本轮目标:

生成进入真实数据 batch 的准备报告。V05 只决定 runtime 是否 ready，不接真实市场数据。

本轮任务:
- 执行任务:
  - 生成 real data handoff readiness artifact。
  - 字段包含 runtime_backend_status、recommended_real_data_backend、required_endpoint_env、source_guardrails、next_stage。
  - 如果 LangGraph 成功，推荐 backend=langgraph_backend；否则推荐 backend=local_fixture。
- 测试任务:
  - 测试 readiness 不声称真实数据已跑。
  - 测试 production_activation_allowed=false。
- 协作验收任务:
  - 生成 `.piko/summaries/worker_V05-5-R01.md`。

允许修改:

- `packages/*`
- `tests/*`
- `docs/*`
- `artifacts/v05_langgraph_install/*`
- `.piko/summaries/worker_V05-5-R01.md`
- `.piko/round_status.json`

禁止修改:

- 不配置真实 endpoint。
- 不执行真实数据采集。

必须运行的验证:

- Real data readiness tests

完成定义:

- 下一步真实数据 batch 的前置条件清楚。

输出格式:
- 修改了什么
- 每个任务状态
- 验证结果
- 协作验收结果
- 未完成/风险
- 下一轮建议
