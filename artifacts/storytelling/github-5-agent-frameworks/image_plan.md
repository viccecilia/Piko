# Image Plan: GitHub 5 Agent Frameworks

Use these as image cards for Xiaohongshu, WeChat illustrations, and future video scenes.

## Image 01 Cover

Text:

```text
别再乱学 Agent 了
真正该懂的是这 5 类
```

Visual:

Dark technical dashboard. Center title. Background shows many blurred GitHub repo cards collapsing into five bright cards.

## Image 02 Project Overload

Text:

```text
Agent 项目太多
收藏越多，越不知道从哪学
```

Visual:

Messy stack of repo cards: LangGraph, CrewAI, AutoGen, LlamaIndex, Dify, Semantic Kernel, Pydantic AI. Add red question marks and a cluttered flow.

## Image 03 The Funnel

Text:

```text
不是收藏 100 个项目
而是筛出 5 个位置
```

Visual:

Funnel diagram: GitHub high-star projects -> relevance scoring -> 5 capability positions.

## Image 04 Five Positions Map

Text:

```text
流程 / 证据 / 分工 / 实验 / 应用
```

Visual:

Five-column capability map. Each column has one icon: flow nodes, database, people, chat bubbles, app window.

## Image 05 LangGraph Card

Text:

```text
LangGraph
流程和状态
```

Visual:

Node graph with checkpoints, retry arrows, gate icon.

## Image 06 LlamaIndex Card

Text:

```text
LlamaIndex
资料和证据
```

Visual:

Source cards flowing into evidence cards, then into an answer with citation lines.

## Image 07 CrewAI Card

Text:

```text
CrewAI
角色和协作
```

Visual:

Six agent avatars: Source, Evidence, Ranking, Writer, Editor, Factcheck. Arrows between them.

## Image 08 AutoGen / AG2 Card

Text:

```text
AutoGen / AG2
多 Agent 实验场
```

Visual:

Conversation threads between multiple agents, with experiment labels and result checkpoint.

## Image 09 Dify / OpenAI Agents SDK Card

Text:

```text
Dify / OpenAI Agents SDK
应用和工具入口
```

Visual:

Tool icons connected to a clean app interface: search, database, browser, file, API.

## Image 10 Wrong Question

Text:

```text
别问哪个最强
问它负责哪个位置
```

Visual:

Split screen. Left side: trophy fight. Right side: ordered production line.

## Image 11 Piko Route

Text:

```text
成熟框架 + Piko 规则
证据链 + 验证 + 人类确认
```

Visual:

Pipeline: framework -> Piko rules -> evidence -> verification -> human approval.

## Image 12 No Model Training

Text:

```text
不训练模型
不从零造 runtime
```

Visual:

Two crossed-out cards: model training, full custom runtime. Highlight card: mature frameworks.

## Image 13 Daily GitHub Scan

Text:

```text
每天扫描高星项目
发现新能力候选
```

Visual:

Calendar plus GitHub-style repo list. Highlight stars >= 5000 and "candidate".

## Image 14 CAP + STORY Handoff

Text:

```text
CAP 判断能不能升级
STORY 生成图文和视频
```

Visual:

Branching diagram: OSS -> CAP capability map, OSS -> STORY content package.

## Image 15 Human Final Gate

Text:

```text
系统提出候选
人类最后确认
```

Visual:

Human approval button at final gate, with draft cards waiting.

## Image 16 Closing Card

Text:

```text
学 Agent
先看懂系统位置
再看项目名字
```

Visual:

Five cards align into one clean architecture board.
