# Short Video Storyboard

Format: 9:16, 60-75 seconds, clean technical knowledge style.

## Scene 01 Hook, 0-4s

Screen text:

```text
别再乱学 Agent 了
真正该懂的是这 5 类
```

Visual:

Many repo cards collapse into five clean cards.

Voice:

大多数人学 Agent，第一步就错了。

## Scene 02 Pain, 4-10s

Screen text:

```text
项目越收藏
思路越混乱
```

Visual:

Messy GitHub repo wall with names flying in.

Voice:

他们一上来就收藏项目。越看越多，越学越乱。

## Scene 03 Turn, 10-16s

Screen text:

```text
不是学 100 个项目
而是看懂 5 个位置
```

Visual:

Funnel turns many projects into five positions.

Voice:

我看完一圈高星项目后发现，真正该先搞懂的是 5 类能力。

## Scene 04 Five Map, 16-22s

Screen text:

```text
流程 / 证据 / 分工 / 实验 / 应用
```

Visual:

Five-column map.

Voice:

流程、证据、分工、实验、应用。

## Scene 05 LangGraph, 22-29s

Screen text:

```text
LangGraph
流程和状态
```

Visual:

Node graph with retries and gate.

Voice:

LangGraph 解决的是下一步谁做，状态放哪里，失败怎么重试。

## Scene 06 LlamaIndex, 29-36s

Screen text:

```text
LlamaIndex
资料和证据
```

Visual:

Source records turn into evidence cards.

Voice:

LlamaIndex 解决的是资料从哪里来，回答能不能追溯。

## Scene 07 CrewAI, 36-43s

Screen text:

```text
CrewAI
角色和协作
```

Visual:

Six role cards connected.

Voice:

CrewAI 把复杂工作拆成多个角色。

## Scene 08 AutoGen, 43-50s

Screen text:

```text
AutoGen / AG2
多 Agent 实验
```

Visual:

Agent conversation graph.

Voice:

AutoGen 适合观察多个 Agent 怎么对话、协商和调用。

## Scene 09 Application, 50-58s

Screen text:

```text
Dify / OpenAI Agents SDK
应用和工具入口
```

Visual:

Tool icons connect into app UI.

Voice:

Dify 和 OpenAI Agents SDK，更接近把 Agent 做成能用的应用。

## Scene 10 Piko Route, 58-68s

Screen text:

```text
成熟框架 + Piko 规则
证据链 + 验证 + 人类确认
```

Visual:

Pipeline board.

Voice:

这也是 Piko 的路线。不训练模型，不从零造 runtime，用成熟框架，加上证据链和验证。

## Scene 11 Close, 68-75s

Screen text:

```text
先看系统位置
再看项目名字
```

Visual:

Five cards lock into one architecture.

Voice:

真正能落地的 Agent，不是一个工具，是一条能被验证的生产线。
